export function onScreenChange(newScreenNumber:number){
    //if(newScreenNumber > screens.length-1) throw "screen out of range";
    console.log("not yet implemented, screen change req for " +
                newScreenNumber);
}